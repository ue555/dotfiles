# Vim の設定

## Quickstart

### 0.設定ファイル

> zshにパスを設定する

```shell
vi ~/.zprofile
```

### 1.最新版の Vim をインストールする

> Mac に Vim をインストールする

```shell
brew install vim 

# 更新時はこれだけで OK
brew update vim

brew upgrade vim
```

